#[cfg(target_family = "unix")]

mod std::net::unix;

import std::sys;

pub func getaddrinfo(name: *u8, service: *u8, hints: *sys::DnsEntry, result: mut **sys::DnsEntry) i32 {
    return sys::getaddrinfo(name, service, hints, result);
}
