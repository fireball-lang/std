mod std;

import std::os::{stderr};
import std::c::{exit};

#[init]
func init_panic() {
    set_panic_handler(panic_handler);
}

func panic_handler(msg: StringView) {
    var w = stderr();

    w.write("panic: ");
    w.write(msg);
    w.write("\n");

    exit(1);
}
