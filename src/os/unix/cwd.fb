#[cfg(target_family = "unix")]

mod std::os::unix;

import std::c::{from_cstring};
import std::sys;

pub func get_cwd(buffer: mut []u8) ?u64 {
    if (sys::getcwd(buffer.ptr, buffer.size) == null) return Option:[u64]::none();
    return from_cstring(buffer.ptr).size;
}
